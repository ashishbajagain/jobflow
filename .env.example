# Auth
AUTH_SECRET=generate-a-long-random-string-here
APP_URL=http://localhost:3000

# Default seeded user (development only — change after first login)
DEFAULT_USER_PASSWORD=JobFlow@Ashish2026

# Password reset emails (optional — logs to console in dev without this)
# RESEND_API_KEY=
# EMAIL_FROM=JobFlow <noreply@yourdomain.com>
